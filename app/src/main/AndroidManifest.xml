<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".base.BaseApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Alfardan"
        tools:targetApi="31">
        <activity
            android:name=".ui.presentation.sendmoney.currencypicker.CurrencyPickerActivity"
            android:exported="false" />
        <activity
            android:name=".ui.presentation.sendmoney.SendMoneyActivity"
            android:exported="false" />
        <activity
            android:name=".ui.presentation.home.HomeActivity"
            android:exported="false" />
        <activity
            android:name=".ui.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".ui.presentation.auth.login.AuthLoginActivity" />
        <activity android:name=".ui.presentation.auth.register.AuthRegisterActivity" />
    </application>

</manifest>